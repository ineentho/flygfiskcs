general:
  branches:
    ignore:
      - gh-pages
dependencies:
  pre:
    - npm install -g broccoli-cli
test:
  override:
    -
deployment:
  master:
    branch: master
    commands:
      - ./deploy